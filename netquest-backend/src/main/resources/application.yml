spring:
  application:
    name: netquest-backend
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://vs1147.dei.isep.ipp.pt:3306/netquest
    username: root
    password: +1rjOGSA8Fjd

app:
  cors:
    allowed-origins: http://localhost:3000, https://netquest-dev-environment.onrender.com, https://netquest-labdsof-prod.onrender.com

logging:
  level:
    org.springframework.security: DEBUG
    # org.hibernate.SQL: DEBUG
