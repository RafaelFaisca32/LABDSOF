spring:
  application:
    name: book-api
  jpa:
    hibernate:
      ddl-auto: create
      dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://vsgate-s1.dei.isep.ipp.pt:10406/netquest
    username: root
    password: htLD7J9wkLaS
app:
  cors:
    allowed-origins: http://localhost:8080

logging:
  level:
    org.springframework.security: DEBUG
    # org.hibernate.SQL: DEBUG
